export interface Products {
    id: string;
    name: string;
    description: string;
    dimensions: string;
    images: string[];
    price: number;
    quantity: number;
    // Define other fields in your document here
}
